'use strict'

// const mist3 = require('node-mist3')
/* const BitkHeader = require('./BitkHeader')

exports = (sequences) => {
    const loci = sequences.map( seq => {
        const bitkHeader = new BitkHeader(seq.header)
        return bitkHeader.getLocus()
    })

} */
